import 'dart:ui';

class DarkColor {
  // Мягкий тёмный фон (не угольно-чёрный, а бархатный)
  static final Color bgColor = Color(0xFF0F1115);

  // Активный синий — более мягкий и глубокий (не неон)
  static final Color activeColor = Color(0xFF4C8DF7);

  // Текст заголовков — дымчато-серый, не ярко-белый
  static final Color titleTextColor = Color(0xFFE6E6E6);

  // Подзаголовки — мягкий сероватый с лёгкой синевой
  static final Color subTitleTextColor = Color(0xFF9FA6B2);

  // Цвет "online" — зелёный пастельный, без ядовитости
  static final Color onlineColor = Color(0xFF32D47C);
}
